#include <stdio.h>
    struct point
    {
        int x;
        int y;
    };
    struct point *r;
   void  main()
   {
    // r = (point *) malloc(sizeof(struct point));
    // r = (point *) malloc(sizeof(struct point));
    // r.x = 1; 
     r->x = 1;
    }
